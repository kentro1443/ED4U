node_modules
.next
dist
coverage
apps/web/src/generated
packages/mentor-engine
packages/facility-engine/dist
packages/facility-engine/dist
benchmark/reports
package-lock.json
*.tgz
playwright-report
test-results
storage
PLAN.md

# Rewritten by `next dev`/`next build` on every boot; formatting it is a
# permanent race with the framework, so it is machine-owned.
apps/web/tsconfig.json

# Local browser-automation artifacts.
.playwright-mcp
